let pulgadas = 0.0254;
let metros = Number(prompt("Ingrese los metros a convertir a pulgadas"));
let resultado = metros * (pulgadas / 1);
alert(`La conversion de metros a pulgadas es: ${resultado}`);
